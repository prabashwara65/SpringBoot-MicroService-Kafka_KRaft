# Change port to 8081 (as per your architecture)
server.port=8081

# Application name
spring.application.name=item-service

# Disable default error handling for cleaner responses
server.error.include-message=always